# AKO_visual_design_agent 白皮书

> **文档版本**：v1.0  
> **发布日期**：2026-07-28  
> **作者**：AKO_studio  
> **密级**：内部规范  
> **定位**：AKO全系产品视觉资产的设计中枢

---

## 一、引言：为什么需要专门的视觉设计Agent

### 1.1 问题诊断

当前AI设计工具的通病：

| 病症 | 表现 | 后果 |
|---|---|---|
| **无设计系统** | 每次输出风格飘忽，没有统一的网格、字号、色彩层级 | 品牌认知碎片化 |
| **无语义设计** | 图标只是"好看"，不承载功能暗示 | 用户学习成本高 |
| **无场景适配** | 不考虑安装包、任务栏、暗色模式等不同场景的显示差异 | 某些场景下图标糊成一团 |
| **无品牌基因** | 输出物放之四海皆准，去掉Logo分不清是谁家的 | 无法建立品牌溢价 |

AutoCAD、Adobe、Figma 这些老牌专业软件的核心竞争力之一，就是**数十年沉淀的设计系统**。AKO_visual_design_agent 的使命，就是把AKO的设计系统固化成可执行的规则引擎。

### 1.2 设计哲学

> **"每一个像素都是AKO的品牌宣言。"**

- **克制**：少即是多，拒绝堆砌
- **语义**：每个图标、每种颜色都在说话
- **一致**：全系产品共用同一套视觉语言
- **场景**：同一资产在不同尺寸、不同背景下都有最优表现

---

## 二、Agent架构

### 2.1 名称与定位

- **正式名称**：`AKO_visual_design_agent`
- **简称**：`AKO_vd_agent`
- **定位**：AKO全系产品的视觉资产设计师与品牌守门人
- **上游输入**：`AKO_hub`（接收产品定义）、`AKO_pack_agent`（接收打包需求）
- **下游输出**：`AKO_pack_agent`（提供视觉资产用于打包）

### 2.2 核心能力矩阵

```
┌─────────────────────────────────────────────────────────────┐
│                    AKO_visual_design_agent                   │
├─────────────┬─────────────┬─────────────┬─────────────────────┤
│  图标设计    │  界面配色    │  向导图设计  │   UI组件风格指南     │
├─────────────┼─────────────┼─────────────┼─────────────────────┤
│ • .ico多分辨率│ • 主色/辅色  │ • 安装向导图 │ • 按钮/输入框/卡片   │
│ • 任务栏图标 │ • 背景色    │ • 欢迎页大图 │ • 字体层级          │
│ • 托盘图标   │ • 状态色    │ • Splash Screen│ • 间距网格         │
│ • 暗色适配   │ • 渐变方案   │ • 品牌水印   │ • 动效规范          │
└─────────────┴─────────────┴─────────────┴─────────────────────┘
```

---

## 三、设计系统核心规范（AKO Design System）

### 3.1 色彩系统

继承并扩展AKO品牌色系，建立完整的色彩层级：

#### 3.1.1 品牌主色（Brand Primary）

| 角色 | 名称 | RGB | HEX | 用途 |
|---|---|---|---|---|
| 主色 | 奶油金 | (235,218,185) | #EBDAB9 | 大面积底色、启动画面背景 |
| 辅色 | 琥珀金 | (160,140,100) | #A08C64 | 按钮、高亮、图标主色 |
| 强调 | 熔金 | (185,155,95) | #B99B5F | 标题、关键词、hover状态 |

#### 3.1.2 功能色（Functional Colors）

| 角色 | RGB | HEX | 用途 |
|---|---|---|---|
| 成功 | (120,180,140) | #78B48C | 操作成功、在线状态 |
| 警告 | (220,180,100) | #DCB464 | 需要注意、待确认 |
| 错误 | (200,100,90) | #C8645A | 操作失败、离线状态 |
| 信息 | (140,160,180) | #8CA0B4 | 提示信息、链接 |

#### 3.1.3 中性色（Neutral Scale）

建立9级灰度，从深到浅：

| 层级 | 名称 | RGB | HEX | 用途 |
|---|---|---|---|---|
| N900 | 深棕黑 | (35,30,28) | #231E1C | 主文字、标题、Logo深色版 |
| N800 | 炭棕 | (55,48,44) | #37302C | 次级标题 |
| N700 | 深灰棕 | (85,78,72) | #554E48 | 正文文字 |
| N600 | 中灰棕 | (120,112,104) | #787068 | 辅助文字、禁用状态 |
| N500 | 冷暖灰 | (195,190,180) | #C3BEB4 | 边框、分割线、背景 |
| N400 | 浅灰 | (210,205,195) | #D2CDC3 | 次级背景 |
| N300 | 淡灰 | (225,220,210) | #E1DCD2 | 卡片背景 |
| N200 | 极浅灰 | (240,235,225) | #F0EBE1 | 页面背景 |
| N100 | 近白 | (250,248,244) | #FAF8F4 | 纯白替代色 |

> **铁律**：AKO全系产品禁止使用纯黑#000000和纯白#FFFFFF作为大面积色。纯白仅用于极小面积的高对比文字。

### 3.2 图标系统

#### 3.2.1 图标风格

- **风格**：线性图标（Line Icon）为主，面性图标（Fill Icon）为辅
- **线宽**：2px（标准），1.5px（小尺寸），2.5px（大尺寸）
- **端点**：圆角（Round Cap）
- **拐角**：圆角（Round Join）
- **网格**：24×24px 设计网格，关键节点对齐到整数像素

#### 3.2.2 图标语义规范

每个图标必须承载明确的功能语义，禁止"为了好看而设计"：

| 功能 | 图标语义 | 设计要点 |
|---|---|---|
| 安装 | 向下箭头+盒子 | 箭头指向打开的盒子，暗示"放入" |
| 启动 | 播放三角形 | 圆角三角形，暗示"开始运行" |
| 设置 | 齿轮 | 6齿，暗示"可调节" |
| 帮助 | 问号+对话框 | 暗示"有疑问找我" |
| 退出 | 门+向外箭头 | 暗示"离开"而非"关闭" |
| 保存 | 软盘（经典语义）| 老用户秒懂，新用户通过上下文理解 |

#### 3.2.3 多分辨率输出规范

.ico文件必须包含以下分辨率：

```
┌──────────┬─────────────┬─────────────────────────────┐
│  尺寸     │   使用场景   │         设计要点             │
├──────────┼─────────────┼─────────────────────────────┤
│ 256×256  │  桌面/大图标  │ 完整细节，可含文字/版本号     │
│ 128×128  │  文件资源管理器│ 中等细节，保持辨识度          │
│ 64×64    │  列表视图    │ 简化细节，核心图形突出        │
│ 48×48    │  任务栏/工具栏│ 极简图形，2px线宽             │
│ 32×32    │  窗口标题栏  │ 单色系简化版，保证清晰度      │
│ 24×24    │  菜单栏     │ 纯线性，无填充               │
│ 16×16    │  系统托盘/状态栏│ 最简几何，仅保留轮廓        │
└──────────┴─────────────┴─────────────────────────────┘
```

> **铁律**：16×16px图标必须在像素级精确对齐，禁止抗锯齿导致的模糊。

### 3.3 字体系统

| 层级 | 字号 | 字重 | 行高 | 用途 |
|---|---|---|---|---|
| H1 | 32px | Bold | 1.3 | 启动画面标题、安装向导欢迎页 |
| H2 | 24px | SemiBold | 1.4 | 窗口标题、分区标题 |
| H3 | 18px | Medium | 1.5 | 卡片标题、功能模块名 |
| Body | 14px | Regular | 1.6 | 正文、说明文字 |
| Caption | 12px | Regular | 1.5 | 辅助说明、时间戳、版本号 |
| Label | 11px | Medium | 1.4 | 按钮文字、标签 |

**字体栈**：
```css
/* Windows */
font-family: "Microsoft YaHei", "PingFang SC", "Noto Sans SC", sans-serif;
/* macOS */
font-family: "PingFang SC", "Hiragino Sans GB", "Noto Sans SC", sans-serif;
```

### 3.4 间距与网格

- **基础单位**：4px
- **网格系统**：8px网格（所有间距为8的倍数：8, 16, 24, 32, 40, 48, 64）
- **圆角规范**：
  - 小元素（按钮、输入框）：4px
  - 中元素（卡片、弹窗）：8px
  - 大元素（窗口、面板）：12px
  - 圆形元素（头像、状态点）：50%

### 3.5 动效规范

| 场景 | 时长 | 缓动函数 | 说明 |
|---|---|---|---|
| 按钮Hover | 200ms | ease-out | 颜色/阴影变化 |
| 窗口打开 | 300ms | cubic-bezier(0.4, 0, 0.2, 1) | 缩放+淡入 |
| 页面切换 | 250ms | ease-in-out | 滑动过渡 |
| 加载动画 | 循环 | linear | 旋转或脉冲 |
| Splash Screen | 3000ms | ease-in-out | 品牌动画固定时长 |

---

## 四、Agent工作流

### 4.1 输入接口

AKO_visual_design_agent 接收以下输入：

```json
{
  "agent_name": "quote_agent",
  "agent_display_name": "报价智能体",
  "version": "2.1.0",
  "client_name": "中黔顺安建设有限公司",
  "client_short": "中黔顺安",
  "product_type": "desktop_app",
  "window_size": "1200x800",
  "features": ["报价计算", "材料清单", "PDF导出", "历史记录"],
  "special_requirements": "需要体现建筑行业属性"
}
```

### 4.2 处理流程

```
Step 1: 解析需求
    ↓ 提取产品类型、功能列表、品牌约束
Step 2: 色彩方案生成
    ↓ 基于AKO品牌色系，生成功能色分配方案
Step 3: 图标设计
    ↓ 为每个核心功能设计语义图标，输出多分辨率.ico
Step 4: 界面框架设计
    ↓ 生成窗口布局、组件样式、配色应用
Step 5: 向导图/Splash设计
    ↓ 基于品牌模板，填充产品信息
Step 6: 资产导出
    ↓ 按AKO_pack_agent要求的目录结构输出
Step 7: 质量检查
    ↓ 自动检查：分辨率完整性、色彩合规性、文件命名规范
Step 8: 交付
    ↓ 输出至 /mnt/agents/output/visual_assets/{agent_name}/
```

### 4.3 输出资产清单

| 资产 | 格式 | 尺寸 | 命名规范 |
|---|---|---|---|
| 安装包图标 | .ico | 16-256px多分辨率 | `AKO_{agent}_icon_v{ver}.ico` |
| 任务栏图标 | .ico | 16-48px | `AKO_{agent}_taskbar_v{ver}.ico` |
| 托盘图标 | .ico | 16-32px | `AKO_{agent}_tray_v{ver}.ico` |
| 安装向导左图 | .bmp | 164×314px | `wizard_left_{agent}_{client}.bmp` |
| 安装向导大图 | .bmp | 500×300px | `wizard_welcome_{agent}_{client}.bmp` |
| Splash Screen | .png | 600×400px | `splash_{agent}_{client}_v{ver}.png` |
| 界面配色方案 | .json | - | `theme_{agent}_v{ver}.json` |
| UI组件样式指南 | .md + .png | - | `ui_guide_{agent}_v{ver}.md` |
| 功能图标集 | .png/.svg | 24×24px | `icon_{function}_{agent}.svg` |

### 4.4 与AKO_pack_agent的协作接口

```
AKO_visual_design_agent          AKO_pack_agent
        │                              │
        │  ① 输出视觉资产包              │
        │ ───────────────────────────> │
        │  /visual_assets/{agent}/     │
        │                              │
        │  ② 确认资产完整性              │
        │ <─────────────────────────── │
        │  {status: "complete", list:[]}│
        │                              │
        │  ③ 开始打包                   │
        │                              │
```

**协作规则**：
- AKO_pack_agent 在打包前必须调用 AKO_visual_design_agent 检查视觉资产完整性
- 若资产缺失，pack_agent 暂停打包，返回错误清单
- 视觉资产通过审核后，pack_agent 将其嵌入安装包对应位置

---

## 五、质量门禁

### 5.1 自动检查项

AKO_visual_design_agent 输出前必须通过的自动检查：

| 检查项 | 标准 | 失败处理 |
|---|---|---|
| 色彩合规 | 所有色值必须在AKO色彩系统内 | 标红警告，禁止输出 |
| 分辨率完整 | .ico必须包含16/24/32/48/64/128/256px | 缺失则自动补全 |
| 文件命名 | 必须符合AKO命名规范 | 自动重命名 |
| 文件大小 | .ico ≤ 500KB，.bmp ≤ 200KB，.png ≤ 300KB | 压缩优化 |
| 像素对齐 | 16×16图标必须像素级对齐 | 自动修正 |

### 5.2 人工Review项

以下项目必须由AKO_studio人工确认：

- [ ] 图标语义是否准确传达功能
- [ ] 配色方案在目标显示器上的实际效果
- [ ] 暗色模式适配是否到位
- [ ] 品牌水印位置是否恰当
- [ ] 业主公司名拼写是否正确

---

## 六、技术实现路径

### 6.1 核心依赖

```
Pillow          - 图像处理与.ico生成
cairoSVG        - SVG渲染
qrcode          - 二维码生成（用于售后卡片）
Jinja2          - 模板引擎（向导图文案填充）
```

### 6.2 图标生成逻辑

不依赖通用AI生成，而是基于**AKO图标组件库**组合生成：

```python
# 伪代码示例
from AKO_icon_kit import IconBuilder, AKO_COLORS

icon = IconBuilder(
    base_shape="hexagon",           # 基础形状：六边形（暗示建筑/结构）
    inner_symbol="calculator",      # 内部符号：计算器（暗示报价功能）
    primary_color=AKO_COLORS["amber_gold"],
    secondary_color=AKO_COLORS["cream_gold"],
    size=256
)
icon.generate_multi_resolution(
    sizes=[16, 24, 32, 48, 64, 128, 256],
    output="AKO_quote_agent_icon_v2.1.0.ico"
)
```

**核心思想**：不是让AI"画"图标，而是让Agent从AKO预定义的**图标语义库**中组合、配色、导出。保证风格一致性和语义准确性。

### 6.3 向导图生成逻辑

基于Jinja2模板 + Pillow合成：

```python
from AKO_wizard_kit import WizardGenerator

wizard = WizardGenerator(
    template="standard",            # 标准模板
    agent_name="报价智能体",
    client_name="中黔顺安",
    version="2.1.0",
    accent_color=AKO_COLORS["amber_gold"]
)
wizard.generate_left_panel("wizard_left_quote_中黔顺安.bmp")
wizard.generate_welcome_page("wizard_welcome_quote_中黔顺安.bmp")
```

---

## 七、演进路线

### Phase 1：基础资产（v1.0，当前）
- [ ] 建立AKO色彩系统（已完成）
- [ ] 建立图标语义库（基础20个图标）
- [ ] 建立向导图模板（3套）
- [ ] 建立Splash Screen模板（2套）
- [ ] 实现.ico多分辨率生成

### Phase 2：界面系统（v1.5）
- [ ] 建立UI组件库（按钮、输入框、卡片、表格）
- [ ] 建立窗口布局模板（5种常见布局）
- [ ] 实现暗色模式自动适配
- [ ] 建立动效规范库

### Phase 3：智能生成（v2.0）
- [ ] 基于功能描述自动生成图标语义匹配
- [ ] 基于业主行业属性自动调整配色权重
- [ ] 基于窗口尺寸自动优化布局
- [ ] 与AKO_pack_agent完全自动化对接

### Phase 4：设计验证（v2.5）
- [ ] 集成可访问性检查（色盲友好、对比度）
- [ ] 集成多显示器适配验证
- [ ] 集成用户眼动热力图分析（如有条件）

---

## 八、附录

### 附录A：与相关Agent的关系图

```
                    AKO_hub
                      │
        ┌─────────────┼─────────────┐
        │             │             │
   AKO_quote_agent  AKO_layout_agent  AKO_netwatch_agent
        │             │             │
        └─────────────┼─────────────┘
                      │
            AKO_visual_design_agent
                      │
        ┌─────────────┴─────────────┐
        │                           │
   AKO_pack_agent              AKO_delivery_agent
   (接收视觉资产打包)            (接收完整交付物)
```

### 附录B：禁用清单

以下设计元素永久禁止出现在AKO产品中：

- ❌ 纯黑#000000大面积使用
- ❌ 纯白#FFFFFF大面积使用
- ❌ 渐变色超过3个色阶
- ❌ 阴影模糊半径超过8px
- ❌ 圆角超过16px（除圆形元素）
- ❌ 使用系统默认字体（如宋体、SimSun）
- ❌ 图标使用渐变色填充
- ❌ 动画时长超过500ms（除Splash Screen）
- ❌ 使用emoji代替图标
- ❌ 未经验证的第三方图标库

### 附录C：参考标杆

| 软件 | 学习点 |
|---|---|
| AutoCAD 2026 | 工具栏密度控制、暗色模式适配、命令行与绘图区比例 |
| Adobe Photoshop | 面板层级、图标语义一致性、状态反馈 |
| Figma | 组件系统、自动布局、设计令牌（Design Token） |
| VS Code | 插件图标规范、主题系统、状态栏信息层级 |

---

> **"设计不是装饰，是功能的一部分。"**  
> —— AKO_studio

---

*本文件由 AKO_studio 编制，纳入 AKO 体系文档标准，受 Git 版本控制。*
