# AKO_visual_design_agent 白皮书

> **文档版本**：v1.2  
> **发布日期**：2026-07-28  
> **作者**：AKO_studio  
> **密级**：内部规范  
> **定位**：AKO全系产品视觉资产的智能设计中枢——功能感知型设计Agent

---

## 一、引言

### 1.1 核心命题

AKO_visual_design_agent 不是"给一个模板填色"的流水线，而是**能读懂Agent功能、分析交互场景、针对性出设计方案**的智能设计中枢。

每个AKO Agent的功能界面截然不同：
- `AKO_quote_agent` → 数字密集、表格为主、计算结果突出
- `AKO_netwatch_agent` → 仪表盘、状态灯、实时数据流、告警闪烁
- `AKO_layout_agent` → 画布区、网格线、拖拽交互、图层管理
- `AKO_writer_agent` → 文档编辑区、侧边栏目录、富文本工具条

visual_design_agent 必须**先分析功能需求，再出针对性方案**，审批通过后交付AKO_pack_agent。未经审批的视觉资产，pack_agent有权拒绝接收。

### 1.2 设计哲学

> **"功能决定形态，形态承载品牌。"**

- **功能感知**：先读懂Agent做什么，再决定界面长什么样
- **场景适配**：安装包、运行时、任务栏、暗色模式，每个场景单独优化
- **品牌锚定**：无论功能如何变化，AKO品牌基因（暖金黄昏调）不可动摇
- **审批闭环**：设计方案必须经AKO_studio审批，审批通过才进入生产

---

## 二、Agent架构：功能感知型四层模型

### 2.1 名称与定位

- **正式名称**：`AKO_visual_design_agent`
- **简称**：`AKO_vd_agent`
- **定位**：AKO全系产品的功能感知型视觉设计师、品牌守门人、设计方案审批流管理者
- **上游输入**：`AKO_hub`（接收产品定义）、各业务Agent（接收功能描述）
- **下游输出**：`AKO_pack_agent`（经审批后的视觉资产包）

### 2.2 四层架构

```
┌────────────────────────────────────────────────────────────────────┐
│  Layer 1: 需求感知层（Perceptor）                                   │
│  ─────────────────────────────────────────────────────────────────  │
│  输入：Agent功能描述 + 交互场景清单 + 用户画像                        │
│  处理：LLM解析功能语义 → 提取界面类型 → 识别关键交互模式              │
│  输出：功能感知报告（functional_perception_report.json）             │
└──────────────────────────┬───────────────────────────────────────────┘
                           │ 功能感知报告
┌──────────────────────────▼───────────────────────────────────────────┐
│  Layer 2: 方案规划层（Planner）                                       │
│  ─────────────────────────────────────────────────────────────────  │
│  输入：功能感知报告 + AKO Design System + 设计意图                     │
│  处理：匹配界面模板 → 定制配色权重 → 规划组件布局 → 生成设计方案       │
│  输出：视觉设计方案草案（design_proposal_v{ver}.md + 小样文件）       │
└──────────────────────────┬───────────────────────────────────────────┘
                           │ 设计方案草案
┌──────────────────────────▼───────────────────────────────────────────┐
│  Layer 3: 审批层（Reviewer）                                          │
│  ─────────────────────────────────────────────────────────────────  │
│  输入：设计方案草案 + 功能感知报告                                     │
│  处理：AKO_studio人工审批 / 规则引擎自动预检                          │
│  输出：审批结果（APPROVED / REVISION_REQUIRED / REJECTED）            │
│  若REVISION_REQUIRED：返回Planner层，携带修改意见                     │
└──────────────────────────┬───────────────────────────────────────────┘
                           │ 审批通过的设计方案
┌──────────────────────────▼───────────────────────────────────────────┐
│  Layer 4: 生产层（Producer）                                         │
│  ─────────────────────────────────────────────────────────────────  │
│  输入：审批通过的设计方案                                             │
│  处理：IconBuilder + ThemeGenerator + WizardKit + SplashKit 执行生产  │
│  输出：完整视觉资产包 + compliance_score + design_decisions_log        │
└────────────────────────────────────────────────────────────────────────┘
```

**核心原则**：
- **Perceptor**负责"读懂功能"，Planner负责"设计方案"，Reviewer负责"把关质量"，Producer负责"精确生产"
- 未经Reviewer审批的设计方案，Producer拒绝执行
- 审批未通过时，Planner必须携带修改意见重新规划，而非直接修改

---

## 三、功能感知系统（Perceptor）

### 3.1 输入接口

```json
{
  "agent_name": "quote_agent",
  "agent_display_name": "报价智能体",
  "version": "2.1.0",
  "client_name": "中黔顺安建设有限公司",
  "client_short": "中黔顺安",
  "product_type": "desktop_app",
  "window_size": "1200x800",
  "features": [
    {
      "name": "报价计算",
      "type": "calculation",
      "ui_pattern": "form + result_panel",
      "data_density": "high",
      "interaction": "input → calculate → display"
    },
    {
      "name": "材料清单",
      "type": "data_table",
      "ui_pattern": "editable_table",
      "data_density": "very_high",
      "interaction": "crud_operations"
    },
    {
      "name": "PDF导出",
      "type": "export",
      "ui_pattern": "button + progress + save_dialog",
      "data_density": "low",
      "interaction": "trigger → process → complete"
    },
    {
      "name": "历史记录",
      "type": "history",
      "ui_pattern": "list + detail_view",
      "data_density": "medium",
      "interaction": "select → view → compare"
    }
  ],
  "interaction_scenarios": [
    "首次启动引导",
    "日常高频操作",
    "数据批量处理",
    "结果导出分享"
  ],
  "user_profile": {
    "role": "造价工程师",
    "technical_level": "intermediate",
    "daily_usage_hours": 4,
    "primary_device": "desktop_1920x1080"
  },
  "design_intent": {
    "industry_tone": "construction",
    "style_variance": "conservative",
    "lighting_mode": "light",
    "accent_preference": "warm"
  },
  "trace_id": "AKO-VD-20260728-001"
}
```

### 3.2 功能感知分析引擎

Perceptor 基于功能描述，自动识别**界面类型**和**关键设计约束**：

#### 3.2.1 界面类型识别矩阵

| 功能类型 | 典型UI模式 | 界面类型 | 设计重点 |
|---|---|---|---|
| calculation | 表单+结果面板 | **计算型界面** | 数字突出、结果区视觉权重高、输入校验反馈 |
| data_table | 可编辑表格 | **数据密集型界面** | 行高优化、列宽自适应、排序筛选控件、分页 |
| chart/dashboard | 图表+指标卡 | **仪表盘型界面** | 实时刷新、状态色编码、告警动画、响应式布局 |
| form/wizard | 多步表单 | **流程型界面** | 进度指示、步骤导航、验证状态、前后跳转 |
| canvas/editor | 画布+工具条 | **编辑型界面** | 网格线、缩放控件、图层管理、撤销重做 |
| document | 文档+目录 | **文档型界面** | 富文本工具条、侧边目录、版本对比、批注 |
| map/3d | 地图/3D视图 | **空间型界面** | 导航控件、比例尺、图层切换、测量工具 |
| chat/dialog | 对话+输入 | **对话型界面** | 消息气泡、输入框、历史滚动、快捷回复 |

#### 3.2.2 关键设计约束提取

Perceptor 从功能描述中提取以下约束，写入功能感知报告：

```json
{
  "functional_perception_report": {
    "trace_id": "AKO-VD-20260728-001",
    "interface_type": "calculation + data_intensive",
    "primary_ui_pattern": "form_left_result_right",
    "data_density": "very_high",
    "critical_interactions": [
      "数字输入即时校验",
      "计算结果高亮显示",
      "表格行内编辑",
      "PDF导出进度反馈"
    ],
    "visual_priorities": [
      "数字可读性 > 装饰性",
      "表格行高 ≥ 40px（容纳编辑控件）",
      "结果区与输入区视觉分离",
      "导出状态需要明确进度指示"
    ],
    "color_usage_rules": [
      "高亮数字使用熔金#B99B5F",
      "校验错误使用错误红#C8645A",
      "成功计算使用成功绿#78B48C",
      "表格斑马纹使用极浅灰#F0EBE1"
    ],
    "icon_requirements": [
      "计算：计算器+六边形",
      "表格：网格+编辑笔",
      "导出：PDF文档+向下箭头",
      "历史：时钟+列表"
    ],
    "accessibility_notes": [
      "数字字体使用等宽字体（如Consolas）",
      "表格需要支持键盘导航",
      "高对比度模式适配"
    ]
  }
}
```

---

## 四、方案规划系统（Planner）

### 4.1 设计方案草案结构

Planner 基于功能感知报告，生成完整的设计方案草案：

```
design_proposal_v1.0.md
├── 1. 设计概述
├── 2. 界面架构图（文字描述+ASCII示意图）
├── 3. 色彩方案（含功能色分配）
├── 4. 图标设计方案（含语义说明）
├── 5. 组件规范（按钮/输入框/表格/卡片等）
├── 6. 布局规范（窗口分区、响应式规则）
├── 7. 动效规范（交互反馈、状态切换）
├── 8. 安装包视觉方案（Wizard/Splash/Icon）
├── 9. 小样文件清单
└── 10. 审批检查表
```

### 4.2 功能感知型设计模板库

Planner 内置**功能感知型设计模板**，根据界面类型自动匹配：

#### 模板A：计算型界面（AKO_quote_agent等）

```
┌─────────────────────────────────────────────┐
│  [AKO Logo]  报价智能体 v2.1.0    [设置][?] │  ← 标题栏：深棕黑背景
├──────────────────┬──────────────────────────┤
│                  │                          │
│   输入参数区      │      计算结果区          │
│   (奶油金底色)    │      (琥珀金高亮边框)     │
│                  │                          │
│   ┌──────────┐   │   ┌──────────────────┐  │
│   │ 材料类型  │   │   │ 总报价：¥128,500  │  ← 熔金大字
│   ├──────────┤   │   │      ↑ 实时计算    │
│   │ 工程量    │   │   └──────────────────┘  │
│   ├──────────┤   │                          │
│   │ 单价      │   │   [材料清单表格]          │
│   └──────────┤   │   (斑马纹+行高40px)       │
│   [重新计算]  │   │                          │
│                  │   [导出PDF] [保存历史]    │
├──────────────────┴──────────────────────────┤
│  状态栏：当前项目 | 最后计算时间 | 在线状态   │  ← 冷暖灰背景
└─────────────────────────────────────────────┘
```

**设计要点**：
- 左右分栏：输入区（左，40%）+ 结果区（右，60%）
- 结果数字使用熔金#B99B5F，32px Bold，等宽字体
- 输入校验错误即时反馈：边框变红#C8645A + 抖动动画
- 计算成功：结果区琥珀金边框脉冲动画1秒
- 表格行高≥40px，支持行内编辑，斑马纹极浅灰#F0EBE1

#### 模板B：仪表盘型界面（AKO_netwatch_agent等）

```
┌─────────────────────────────────────────────┐
│  [AKO Logo]  网络监控 v1.0     [告警][设置] │  ← 标题栏：状态色动态
├─────────────────────────────────────────────┤
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐          │
│  │CPU  │ │内存 │ │磁盘 │ │网络 │  ← 指标卡 │
│  │ 78% │ │ 45% │ │ 62% │ │ 1.2G│    琥珀金│
│  │ ↑   │ │ →   │ │ →   │ │ ↓   │    边框  │
│  └─────┘ └─────┘ └─────┘ └─────┘          │
├─────────────────────────────────────────────┤
│  [实时折线图区域]      [告警日志列表]        │
│  (深棕黑背景+琥珀金线条)  (状态色编码)       │
│                                              │
│  正常：冷暖灰    警告：琥珀金闪烁    错误：红闪烁│
├─────────────────────────────────────────────┤
│  状态栏：监控节点数 | 最后刷新 | 告警计数    │
└─────────────────────────────────────────────┘
```

**设计要点**：
- 深色模式优先（监控场景长时间注视）
- 指标卡：琥珀金边框，数值大字体，趋势箭头
- 折线图：深棕黑背景#231E1C，线条琥珀金#A08C64，网格线冷暖灰#C3BEB4
- 告警状态：琥珀金/红色脉冲动画，2秒周期
- 告警日志：状态色圆点 + 时间戳 + 消息摘要

#### 模板C：编辑型界面（AKO_layout_agent等）

```
┌─────────────────────────────────────────────┐
│  [AKO Logo]  布局设计 v1.0  [文件][编辑][视图]│
├──────┬──────────────────────────┬───────────┤
│ 图层  │                          │  属性     │
│ 面板  │      画布区域             │  面板     │
│      │   (奶油金网格背景)         │          │
│  ┌─┐ │                          │  ┌──────┐ │
│  │A│ │   ┌────────────┐         │  │宽度  │ │
│  │B│ │   │  选中元素    │         │  │高度  │ │
│  │C│ │   │  (琥珀金边框) │         │  │角度  │ │
│  └─┘ │   └────────────┘         │  └──────┘ │
│      │                          │  [应用]   │
├──────┴──────────────────────────┴───────────┤
│  状态栏：缩放100% | 画布尺寸 | 选中元素数    │
└─────────────────────────────────────────────┘
```

**设计要点**：
- 三栏布局：左（图层20%）+ 中（画布60%）+ 右（属性20%）
- 画布背景：奶油金#EBDAB9 + 冷暖灰网格线#C3BEB4（8px间隔）
- 选中元素：琥珀金#A08C64 2px实线边框 + 4个角控制点
- 拖拽状态：元素半透明+阴影
- 图层列表：缩略图 + 名称 + 可见性开关

### 4.3 小样文件（Mockup）规范

Planner 输出设计方案时，必须附带**视觉小样**，供AKO_studio审批时直观判断：

| 小样类型 | 格式 | 尺寸 | 内容 | 命名 |
|---|---|---|---|---|
| 主界面小样 | .png | 1200×800（按实际窗口尺寸） | 完整界面布局，含示例数据 | `mockup_main_{agent}_v{ver}.png` |
| 图标集小样 | .png | 800×400 | 所有图标并排展示，含16/32/48px | `mockup_icons_{agent}_v{ver}.png` |
| 安装向导小样 | .png | 600×400 | Wizard各步骤截图拼接 | `mockup_wizard_{agent}_v{ver}.png` |
| 暗色模式小样 | .png | 1200×800 | 主界面暗色版 | `mockup_dark_{agent}_v{ver}.png` |
| 配色方案小样 | .png | 400×600 | 色板展示 + 使用场景标注 | `mockup_palette_{agent}_v{ver}.png` |

> **小样不是最终资产**，是审批用的"设计稿"。审批通过后，Producer才按小样精确生产最终资产。

---

## 五、审批系统（Reviewer）

### 5.1 审批流程

```
Planner输出设计方案草案
           │
           ▼
┌─────────────────────┐
│  规则引擎自动预检      │  ← 30秒内完成
│  • 色彩合规性         │
│  • 命名规范性         │
│  • 文件完整性         │
└─────────────────────┘
           │
           ▼
┌─────────────────────┐
│  AKO_studio人工审批   │  ← 24小时内响应
│  • 功能匹配度         │
│  • 品牌一致性         │
│  • 用户体验直觉       │
│  • 小样视觉判断       │
└─────────────────────┘
           │
    ┌──────┴──────┐
    ▼             ▼
 APPROVED    REVISION_REQUIRED
    │             │
    ▼             ▼
 进入Producer  返回Planner
               携带修改意见
               （最多3轮）
```

### 5.2 审批检查表（AKO_studio用）

```markdown
## 视觉设计方案审批表

- Trace ID: AKO-VD-20260728-001
- Agent: quote_agent
- 版本: v2.1.0
- 提交时间: 2026-07-28T22:30:00+08:00

### 自动预检结果
- [x] 色彩合规（所有色值在AKO系统内）
- [x] 命名规范（符合AKO命名规则）
- [x] 文件完整（小样+方案+日志齐全）
- [ ] 语义匹配（待人工确认）

### 人工审批项
- [ ] **功能匹配度**：界面布局是否匹配Agent核心功能？
- [ ] **品牌一致性**：是否体现AKO暖金黄昏调？奶油金是否被正确使用？
- [ ] **用户体验**：首次使用是否 intuitively 理解？高频操作路径是否顺畅？
- [ ] **小样质量**：小样是否清晰传达设计意图？暗色模式是否适配？
- [ ] **图标语义**：每个图标是否准确传达功能含义？
- [ ] **特殊需求**：业主定制化需求（如公司Logo、行业元素）是否体现？

### 审批结果
- [ ] APPROVED — 进入Producer生产
- [ ] REVISION_REQUIRED — 修改意见：___________
- [ ] REJECTED — 重新规划

审批人：___________  时间：___________
```

### 5.3 修改意见格式

若审批结果为 REVISION_REQUIRED，修改意见必须结构化：

```json
{
  "revision_request": {
    "trace_id": "AKO-VD-20260728-001",
    "revision_round": 1,
    "overall_assessment": "整体方向正确，细节需调整",
    "specific_issues": [
      {
        "item": "主界面布局",
        "issue": "结果区占比过大，输入区拥挤",
        "suggestion": "调整为左45%右55%，输入区增加垂直间距",
        "priority": "high"
      },
      {
        "item": "图标-导出功能",
        "issue": "PDF图标不够直观",
        "suggestion": "使用文档+向下箭头组合，而非单独文档图标",
        "priority": "medium"
      },
      {
        "item": "配色-成功状态",
        "issue": "成功绿在奶油金背景上对比度不足",
        "suggestion": "成功状态改用深棕黑背景+成功绿文字",
        "priority": "medium"
      }
    ],
    "deadline": "2026-07-29T12:00:00+08:00"
  }
}
```

> **最多3轮修改**。超过3轮仍未通过，触发AKO_hub人工介入会议。

---

## 六、生产系统（Producer）

### 6.1 生产触发条件

Producer 仅在以下条件下启动：
- 审批结果 = APPROVED
- 设计方案版本与审批版本一致
- 所有小样文件已确认

### 6.2 生产流程

```
审批通过的设计方案
           │
           ▼
Step 1: 解析设计方案 → 提取所有资产规格
           │
           ▼
Step 2: 图标生产 → IconBuilder按语义组合生成多分辨率.ico
           │
           ▼
Step 3: 主题生产 → ThemeGenerator输出theme.json
           │
           ▼
Step 4: 向导图生产 → WizardKit填充模板渲染.bmp
           │
           ▼
Step 5: Splash生产 → SplashKit渲染品牌动画帧.png
           │
           ▼
Step 6: UI组件生产 → 按组件规范输出样式文件
           │
           ▼
Step 7: 质量检查 → Critic评分 + 生成日志
           │
           ▼
Step 8: 打包输出 → 按AKO_pack_agent目录结构输出
```

### 6.3 输出资产清单（审批后生产版）

| 序号 | 资产 | 格式 | 尺寸 | 命名规范 | 审批依据 |
|---|---|---|---|---|---|
| 1 | 安装包图标 | .ico | 16-256px | `AKO_{agent}_icon_v{ver}.ico` | 图标小样 |
| 2 | 任务栏图标 | .ico | 16-48px | `AKO_{agent}_taskbar_v{ver}.ico` | 图标小样 |
| 3 | 托盘图标 | .ico | 16-32px | `AKO_{agent}_tray_v{ver}.ico` | 图标小样 |
| 4 | 安装向导左图 | .bmp | 164×314px | `wizard_left_{agent}_{client}.bmp` | 向导小样 |
| 5 | 安装向导大图 | .bmp | 500×300px | `wizard_welcome_{agent}_{client}.bmp` | 向导小样 |
| 6 | Splash Screen | .png | 600×400px | `splash_{agent}_{client}_v{ver}.png` | 主界面小样 |
| 7 | 界面配色方案 | .json | - | `theme_{agent}_v{ver}.json` | 配色小样 |
| 8 | UI组件样式指南 | .md + .png | - | `ui_guide_{agent}_v{ver}.md` | 组件规范 |
| 9 | 功能图标集 | .png/.svg | 24×24px | `icon_{function}_{agent}.svg` | 图标小样 |
| 10 | **设计决策日志** | .md | - | `design_decisions_log_{trace_id}.md` | 全链路 |
| 11 | **审批记录** | .md | - | `approval_record_{trace_id}.md` | 审批结果 |

---

## 七、与AKO_pack_agent的协作接口

### 7.1 协作协议

```
AKO_visual_design_agent          AKO_pack_agent
        │                              │
        │  ① 提交视觉资产包              │
        │  {                           │
        │    "assets": [...],          │
        │    "compliance_score": 96,   │
        │    "status": "APPROVED",     │
        │    "approval_record": "...", │
        │    "design_proposal": "...",   │
        │    "trace_id": "..."          │
        │  }                           │
        │ ───────────────────────────> │
        │                              │
        │  ② pack_agent验证              │
        │  • 检查approval_record是否存在  │
        │  • 检查compliance_score ≥ 90    │
        │  • 检查资产清单完整性           │
        │                              │
        │  ③ 验证通过 → 开始打包         │
        │  验证失败 → 拒绝接收 + 告警     │
```

**pack_agent接收铁律**：
- 无`approval_record`的视觉资产，pack_agent**拒绝接收**
- `compliance_score` < 90，pack_agent**拒绝接收**
- 资产清单与设计方案不一致，pack_agent**拒绝接收**

---

## 八、质量门禁与可观测性

### 8.1 自动检查项（Critic）

| 检查项 | 标准 | 扣分 | 失败处理 |
|---|---|---|---|
| 色彩合规 | 所有色值在AKO系统内 | -10 | 标红 |
| 分辨率完整 | .ico含16/24/32/48/64/128/256px | -5 | 自动补全 |
| 文件命名 | 符合AKO命名规范 | 0 | 自动重命名 |
| 文件大小 | .ico≤500KB, .bmp≤200KB, .png≤300KB | -5 | 压缩优化 |
| 像素对齐 | 16×16 Nearest插值 | -10 | 自动修正 |
| 语义匹配 | 图标形状与功能语义库匹配 | -15 | 触发Planner重规划 |
| **审批完整性** | **必须有APPROVED审批记录** | **-100** | **拒绝输出** |

### 8.2 可观测性

- **trace_id**：贯穿Perceptor→Planner→Reviewer→Producer全链路
- **design_decisions_log.md**：记录每一步决策、置信度、修改历史
- **approval_record.md**：记录审批结果、修改意见、审批人
- **compliance_score**：0-100分，决定pack_agent接收策略

---

## 九、设计系统核心规范（AKO Design System）

### 9.1 色彩系统

#### 品牌主色（不可扰动）

| 角色 | 名称 | RGB | HEX | 用途 | 扰动权限 |
|---|---|---|---|---|---|
| 主色 | 奶油金 | (235,218,185) | #EBDAB9 | 大面积底色、启动画面 | ❌ 禁止 |
| 辅色 | 琥珀金 | (160,140,100) | #A08C64 | 按钮、高亮、图标主色 | ⚠️ 明度±5% |
| 强调 | 熔金 | (185,155,95) | #B99B5F | 标题、关键词、hover | ⚠️ 明度±5% |

#### 功能色（可微调）

| 角色 | 基准RGB | 基准HEX | 用途 | 允许扰动 |
|---|---|---|---|---|
| 成功 | (120,180,140) | #78B48C | 操作成功、在线状态 | 色相±10° |
| 警告 | (220,180,100) | #DCB464 | 需要注意、待确认 | 色相±10° |
| 错误 | (200,100,90) | #C8645A | 操作失败、离线状态 | 色相±10° |
| 信息 | (140,160,180) | #8CA0B4 | 提示信息、链接 | 色相±10° |

#### 中性色（9级灰度）

| 层级 | 名称 | RGB | HEX | 用途 |
|---|---|---|---|---|
| N900 | 深棕黑 | (35,30,28) | #231E1C | 主文字、标题、Logo深色版 |
| N800 | 炭棕 | (55,48,44) | #37302C | 次级标题 |
| N700 | 深灰棕 | (85,78,72) | #554E48 | 正文文字 |
| N600 | 中灰棕 | (120,112,104) | #787068 | 辅助文字、禁用状态 |
| N500 | 冷暖灰 | (195,190,180) | #C3BEB4 | 边框、分割线、背景 |
| N400 | 浅灰 | (210,205,195) | #D2CDC3 | 次级背景 |
| N300 | 淡灰 | (225,220,210) | #E1DCD2 | 卡片背景 |
| N200 | 极浅灰 | (240,235,225) | #F0EBE1 | 页面背景、斑马纹 |
| N100 | 近白 | (250,248,244) | #FAF8F4 | 纯白替代色 |

### 9.2 图标系统

- **风格**：线性图标为主，面性为辅
- **线宽**：2px（标准），1.5px（小尺寸），2.5px（大尺寸）
- **端点/拐角**：圆角（Round Cap / Round Join）
- **网格**：24×24px设计网格，关键节点对齐整数像素
- **16×16**：必须使用Nearest插值，像素级对齐

### 9.3 字体系统

| 层级 | 字号 | 字重 | 行高 | 用途 |
|---|---|---|---|---|
| H1 | 32px | Bold | 1.3 | 启动画面标题、安装向导欢迎页 |
| H2 | 24px | SemiBold | 1.4 | 窗口标题、分区标题 |
| H3 | 18px | Medium | 1.5 | 卡片标题、功能模块名 |
| Body | 14px | Regular | 1.6 | 正文、说明文字 |
| Caption | 12px | Regular | 1.5 | 辅助说明、时间戳、版本号 |
| Label | 11px | Medium | 1.4 | 按钮文字、标签 |

**数字字体栈**（计算型/数据密集型界面专用）：
```css
font-family: "Consolas", "Monaco", "Courier New", monospace;
```

### 9.4 间距与网格

- **基础单位**：4px
- **网格系统**：8px网格（8, 16, 24, 32, 40, 48, 64）
- **圆角**：小元素4px / 中元素8px / 大元素12px / 圆形50%

### 9.5 动效规范

| 场景 | 时长 | 缓动函数 |
|---|---|---|
| 按钮Hover | 200ms | ease-out |
| 窗口打开 | 300ms | cubic-bezier(0.4, 0, 0.2, 1) |
| 页面切换 | 250ms | ease-in-out |
| 加载动画 | 循环 | linear |
| Splash Screen | 3000ms | ease-in-out |
| 校验错误 | 300ms | shake（左右抖动） |
| 计算成功 | 1000ms | pulse（边框脉冲） |

---

## 十、演进路线

### Phase 1：功能感知基础（v1.2，当前）
- [x] 四层架构（Perceptor-Planner-Reviewer-Producer）
- [x] 功能感知分析引擎（界面类型识别矩阵）
- [x] 功能感知型设计模板库（3套基础模板）
- [x] 审批系统（人工审批+自动预检）
- [x] 小样文件规范（5种小样类型）
- [x] 与AKO_pack_agent的审批验证接口

### Phase 2：模板扩展（v1.5）
- [ ] 扩展至8套界面模板（增加流程型、文档型、对话型、空间型）
- [ ] UI组件库（按钮、输入框、表格、卡片、图表、画布）
- [ ] 暗色模式自动适配模板
- [ ] 响应式布局规则（窗口缩放适配）

### Phase 3：智能增强（v2.0）
- [ ] VLM辅助验证图标语义匹配度
- [ ] 基于功能描述自动扩展图标语义库
- [ ] 基于用户画像自动调整界面密度和字体大小
- [ ] 与AKO_pack_agent完全自动化对接

### Phase 4：设计验证（v2.5）
- [ ] 可访问性检查（WCAG对比度、色盲友好）
- [ ] 多显示器DPI适配验证
- [ ] 眼动热力图分析（如有条件）

---

## 十一、附录

### 附录A：与相关Agent的关系图

```
                    AKO_hub
                      │
        ┌─────────────┼─────────────┐
        │             │             │
   AKO_quote_agent  AKO_layout_agent  AKO_netwatch_agent
        │             │             │
        │ 功能描述      │ 功能描述      │ 功能描述
        └─────────────┼─────────────┘
                      │
            AKO_visual_design_agent
              │ Perceptor → Planner → Reviewer → Producer
              │ 审批通过后才输出
        ┌─────┴─────────────┐
        │                   │
   AKO_pack_agent      AKO_delivery_agent
   (验证approval_record)  (接收完整交付物)
```

### 附录B：禁用清单（System Prompt硬约束）

- ❌ 纯黑#000000大面积使用
- ❌ 纯白#FFFFFF大面积使用
- ❌ 渐变色超过3个色阶
- ❌ 阴影模糊半径超过8px
- ❌ 圆角超过16px（除圆形元素）
- ❌ 使用系统默认字体（宋体、SimSun）
- ❌ 图标使用渐变色填充
- ❌ 动画时长超过500ms（除Splash）
- ❌ 使用emoji代替图标
- ❌ 未经验证的第三方图标库
- ❌ 奶油金#EBDAB9的任何色相偏移
- ❌ **未经审批的设计方案进入生产**

### 附录C：界面类型-模板映射速查表

| Agent类型 | 主要功能 | 界面类型 | 适用模板 |
|---|---|---|---|
| quote_agent | 报价计算 | 计算型+数据密集 | 模板A |
| netwatch_agent | 网络监控 | 仪表盘型 | 模板B |
| layout_agent | 布局设计 | 编辑型 | 模板C |
| writer_agent | 文档写作 | 文档型 | Phase 2 |
| form_agent | 表单填写 | 流程型 | Phase 2 |
| chat_agent | 智能对话 | 对话型 | Phase 2 |
| map_agent | 地图/3D | 空间型 | Phase 2 |

---

> **"功能决定形态，形态承载品牌。审批不是障碍，是品质的守门人。"**  
> —— AKO_studio

---

*本文件由 AKO_studio 编制，纳入 AKO 体系文档标准，受 Git 版本控制。*
